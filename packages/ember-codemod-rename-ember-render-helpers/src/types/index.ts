type CodemodOptions = {
  projectRoot: string;
  projectType: 'app' | 'v1-addon';
};

type Options = {
  projectRoot: string;
  projectType: 'app' | 'v1-addon';
};

export type { CodemodOptions, Options };
